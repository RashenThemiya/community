import 'package:flutter/material.dart';

class SanitationTicketingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Sanitation Ticketing',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
